import { MantineThemeOverride } from "@mantine/core"

const theme: MantineThemeOverride = {
    colorScheme: 'light',
    headings: {
        sizes: {
            h1: { fontSize: '4rem' },
            h2: { fontSize: '1.5rem' },
            h3: { fontSize: '1.5rem' },
        }
    },
}

export default theme