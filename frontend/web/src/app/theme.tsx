import { MantineThemeOverride } from "@mantine/core"

const theme: MantineThemeOverride = {
    colorScheme: 'light',
    headings: {
        sizes: {
            h1: { fontSize: '4rem' },
            h2: { fontSize: '2.5rem' },
            h3: { fontSize: '2rem' },
        }
    },
}

export default theme