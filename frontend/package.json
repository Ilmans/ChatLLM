{
  "name": "frontend",
  "version": "1.0.0",
  "author": "",
  "main": "index.js",
  "devDependencies": {
    "bun": "^1.0.3",
    "pnpm": "^8.8.0"
  },
  "description": "",
  "keywords": [],
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "web:dev": "pnpm -C web run dev",
    "web:build": "pnpm -C web run build",
    "start": "pnpm -C web run start",
    "dev": "pnpm run web:dev",
    "build": "pnpm run web:build",
    "llm:dev": "pnpm run --filter ./packages/web-llm dev",
    "lint": "eslint .",
    "lint:fix": "eslint ./**/*.ts --fix"
  },
  "workspaces": [
    "packages/*",
    "web"
  ]
}
